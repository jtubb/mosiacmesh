<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
    /Library/LaunchDaemons/com.mosaicmesh.lighttpd.plist

    Deployed by tools/onboard_devices.ps1 step 5.4e via scp. The
    earlier inline-heredoc approach lost double-quote escaping on
    the way through PowerShell -> ssh -> bash, so the plist landed
    malformed. scp-from-local guarantees byte-identical content.

    -D keeps lighttpd in the foreground so launchd's KeepAlive
    can actually track it across crashes. RunAtLoad fires the
    daemon at boot (launchd reads /Library/LaunchDaemons during
    startup). Note: launchctl-over-SSH can't load this on a running
    iOS 5 system, so 5.4e also exec's lighttpd directly for the
    current onboarding pass; this plist takes over on next reboot.
-->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.mosaicmesh.lighttpd</string>
    <key>ProgramArguments</key>
    <array>
        <string>/usr/sbin/lighttpd</string>
        <string>-D</string>
        <string>-f</string>
        <string>/etc/lighttpd/lighttpd.conf</string>
    </array>
    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>
    <key>StandardErrorPath</key>
    <string>/var/log/lighttpd-launchd.log</string>
</dict>
</plist>
