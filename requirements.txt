aiohttp>=3.8.4
beeprint==2.4.10
device-detector==5.0.1
jsonpickle==3.0.1
numpy>=1.21.6
python-dateutil>=2.8
opencv-contrib-python>=4.8.0.74
opencv-python>=4.8.0.74
sockjs==0.13.0
websocket-client==1.6.1
websockets==11.0.3
zeroconf>=0.100.0          # mDNS/Bonjour reverse-lookup fallback for .local devices

# System dependency (not pip-installable): ffmpeg with libx264 must be on PATH
# (used by the server to render perspective-warped per-screen video for SEGMENT/mosaic playback)